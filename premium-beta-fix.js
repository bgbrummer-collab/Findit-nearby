(() => {
  const KEY='findit_premium_beta';
  const $=s=>document.querySelector(s);
  const active=()=>localStorage.getItem(KEY)==='1';
  const FREE=['AI photo identification','Nearby retailer results','Directions & map','Up to 10 km search radius','Basic recent finds','Search exact item online','Search retailer type near you','Copy product name','Share this find'];
  const PREMIUM=['Up to 25 km search radius','Saved Items','Collections','Watchlist','Favourite Stores','Compare Stores','Smart store filters','History+ (up to 50 finds)','Manual product search','Exact Match search','AI retailer search','Premium stats & workspace'];

  function modal(show){const m=$('#premiumModal');if(!m)return;m.classList.toggle('hidden',!show);m.setAttribute('aria-hidden',show?'false':'true')}
  function ensureRuntime(){try{if(typeof premiumState!=='undefined')premiumState.active=true}catch{}document.body.classList.add('premium-active','premium-v10')}
  function syncState(){const on=active();try{if(typeof premiumState!=='undefined')premiumState.active=on}catch{}document.body.classList.toggle('premium-active',on);document.body.classList.toggle('premium-v10',on);$('#premiumStatusBadge')?.classList.toggle('hidden',!on);$('#premiumWorkspaceButton')?.classList.toggle('hidden',!on);$('#premiumHome')?.classList.toggle('hidden',!on);$('#v10CommandCentre')?.classList.toggle('hidden',!on);const drawer=$('#premiumDrawerNav');if(drawer)drawer.style.display=on?'':'none';const limit=on?25:10;try{if(typeof state!=='undefined'&&Number(state.radius)>limit)state.radius=limit;const stored=Math.min(limit,Number(localStorage.getItem('finditRadius')||10));localStorage.setItem('finditRadius',String(stored));['#radiusSelect','#settingsRadius'].forEach(s=>{const el=$(s);if(el&&Number(el.value)>limit)el.value=String(limit)})}catch{}}
  function updateCopy(){const cards=document.querySelectorAll('#premiumModal .premium-plan-card'),free=cards[0],pro=$('#premiumModal .premium-plan-card.premium');if(free?.querySelector('ul'))free.querySelector('ul').innerHTML=FREE.map(x=>`<li>${x}</li>`).join('');if(pro?.querySelector('ul'))pro.querySelector('ul').innerHTML=PREMIUM.map(x=>`<li>${x}</li>`).join('');const p=$('#premiumModal .premium-card > p');if(p)p.textContent='Premium Beta is free while FindIt is being tested. Premium-only tools are separated from the Free plan.';const small=pro?.querySelector('small');if(small)small.textContent='Free during Premium Beta testing. Real payments are disabled.';const b=$('#activatePremiumTester');if(b)b.textContent=active()?'Premium Beta active ✓':'Activate Premium Beta — Free';let back=$('#finditReturnFree');if(active()&&pro&&!back){back=document.createElement('button');back.id='finditReturnFree';back.type='button';back.textContent='Return to Free for testing';back.style.marginTop='10px';back.onclick=e=>{e.preventDefault();localStorage.removeItem(KEY);syncAll();modal(false);scrollTo(0,0)};pro.appendChild(back)}if(!active())back?.remove();$('#v10CommandCentre [data-v10="share"]')?.remove();const save=$('#saveFind');if(save){save.textContent=active()?'♡ Save':'♡ Save ★';save.title=active()?'Save this Find':'Premium feature'}}

  function runV10(a){ensureRuntime();modal(false);if(a==='scan')return $('#finder')?.scrollIntoView({behavior:'smooth',block:'start'});if(a==='manual'&&typeof v10Manual==='function')return v10Manual();if(a==='exact'&&typeof v10Exact==='function')return v10Exact();if(a==='assistant'&&typeof v10Assistant==='function')return v10Assistant();if(a==='collections'&&typeof v10Collections==='function')return v10Collections();if(a==='watchlist'&&typeof v10Watchlist==='function')return v10Watchlist();if(a==='favourites'&&typeof v10FavouriteStores==='function')return v10FavouriteStores();if(a==='stats'&&typeof v10Stats==='function')return v10Stats();if(a==='history'&&typeof v10History==='function')return v10History();if(typeof v10Handle==='function')return v10Handle(a)}
  function runAction(a){ensureRuntime();modal(false);if(a==='find')return $('#finder')?.scrollIntoView({behavior:'smooth',block:'start'});if(a==='saved'&&typeof openTool==='function')return openTool('saved');if(a==='compare'&&typeof openTool==='function')return openTool('compare');if(a==='map'){const r=$('#results');if(r&&!r.classList.contains('hidden')){r.scrollIntoView({behavior:'smooth',block:'start'});setTimeout(()=>$('#mapViewBtn')?.click(),180)}else $('#finder')?.scrollIntoView({behavior:'smooth'});return}try{if(typeof premiumAction==='function')return premiumAction(a)}catch{}}
  function runWorkspace(a){ensureRuntime();modal(false);try{if(typeof closePremiumWorkspace==='function')closePremiumWorkspace()}catch{}if(a==='saved'&&typeof openTool==='function')return openTool('saved');if(a==='compare'&&typeof openTool==='function')return openTool('compare');if(a==='filters'&&typeof openTool==='function')return openTool('filters');if(a==='history')return $('#recent')?.scrollIntoView({behavior:'smooth'});if(a==='challenge')return $('#challengeBtn')?.click();if(a==='map')return runAction('map');if(a==='find')return runAction('find');if(a==='radius')return $('#premiumHome')?.scrollIntoView({behavior:'smooth'})}
  function route(el){if(el.matches('[data-v10]'))return runV10(el.dataset.v10);if(el.matches('[data-premium-action]'))return runAction(el.dataset.premiumAction);if(el.matches('[data-premium-radius]')){ensureRuntime();modal(false);try{if(typeof premiumRadius==='function')return premiumRadius(el.dataset.premiumRadius)}catch{}}if(el.matches('[data-store-sort]')){ensureRuntime();modal(false);try{if(typeof applyPremiumStoreSort==='function')return applyPremiumStoreSort(el.dataset.storeSort)}catch{}}if(el.matches('[data-pw]'))return runWorkspace(el.dataset.pw)}

  // This capture listener runs before the older button handlers in script.js.
  document.addEventListener('click',e=>{
    const el=e.target?.closest?.('[data-v10],[data-premium-action],[data-premium-radius],[data-store-sort],[data-pw],#saveFind,#widenSearch');if(!el)return;
    if(el.id==='widenSearch'&&active())return;
    e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();
    if(!active())return modal(true);
    if(el.id==='saveFind'){ensureRuntime();try{const i=state?.result?.identification;if(!i)return;let arr=[];try{arr=JSON.parse(localStorage.getItem('finditSaved')||'[]')}catch{}arr=[{name:i.name||i.object||'Item',query:i.searchQuery||'',savedAt:new Date().toISOString()},...arr].slice(0,30);localStorage.setItem('finditSaved',JSON.stringify(arr));el.textContent='✓ Saved';setTimeout(()=>el.textContent='♡ Save',900);if(typeof updatePremiumDashboard==='function')updatePremiumDashboard()}catch{}return}
    route(el);
  },true);

  function blockLegacyUpgrade(){
    // Older code calls openPremium() from several places. While Beta is active,
    // those calls must never reopen the upgrade comparison modal.
    try{
      const legacy=typeof openPremium==='function'?openPremium:null;
      const guarded=function(){if(active()){modal(false);return}return legacy?legacy():modal(true)};
      window.openPremium=guarded;
      try{openPremium=guarded}catch{}
    }catch{}
  }
  function syncAll(){syncState();updateCopy();blockLegacyUpgrade()}
  function init(){syncAll();const b=$('#activatePremiumTester');if(b&&!b.dataset.betaFixed){b.dataset.betaFixed='1';b.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();if(!active())localStorage.setItem(KEY,'1');syncAll();modal(false);const wow=$('#premiumWow');wow?.classList.remove('hidden');setTimeout(()=>wow?.classList.add('hidden'),1500);setTimeout(()=>$('#v10CommandCentre')?.scrollIntoView({block:'start'}),120)},true)}$('#closePremium')?.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();modal(false)},true);document.addEventListener('findit:results-rendered',updateCopy);setTimeout(syncAll,300);setTimeout(syncAll,1200)}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();window.addEventListener('pageshow',syncAll);
})();