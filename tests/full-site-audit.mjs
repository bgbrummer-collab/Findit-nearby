import { chromium } from 'playwright';
import fs from 'node:fs';
import path from 'node:path';

const BASE_URL=process.env.FINDIT_URL||'https://findit-nearby.vercel.app/';
const OUT=process.env.AUDIT_OUT||'audit-output';
fs.mkdirSync(OUT,{recursive:true});
const checks=[],failures=[],warnings=[];
function note(name,status='PASS',detail=''){const row={name,status,detail:String(detail||'').slice(0,700)};checks.push(row);console.log(`[${status}] ${name}${detail?` — ${row.detail}`:''}`);if(status==='FAIL')failures.push(row);if(status==='WARN')warnings.push(row)}
async function safe(name,fn,{warn=false}={}){try{const d=await fn();note(name,'PASS',d||'');return true}catch(e){note(name,warn?'WARN':'FAIL',e?.message||String(e));return false}}
async function isVisible(page,sel){try{return await page.locator(sel).first().isVisible({timeout:1200})}catch{return false}}
async function click(page,sel,name,wait=180){return safe(name,async()=>{const el=page.locator(sel).first();if(!await el.isVisible({timeout:2500}))throw Error('control is not visible');await el.click({timeout:4000});if(wait)await page.waitForTimeout(wait)})}
async function closeKnown(page){for(const s of ['#v10CloseModal','#finditGuideModal .findit-guide-close','#premiumSavedModal [data-close-tool="saved"]','#premiumCompareModal [data-close-tool="compare"]','#premiumFiltersModal [data-close-tool="filters"]','#challengeModal [data-close-modal]','#settingsModal [data-close-modal]','#closeAssistant','#closePremium']){try{const e=page.locator(s).first();if(await e.count()&&await e.isVisible())await e.click({timeout:900})}catch{}}try{if(await isVisible(page,'#drawer.open'))await page.locator('#closeMenu').click({timeout:900})}catch{}}
async function expectVisible(page,sel,msg='not visible'){if(!await isVisible(page,sel))throw Error(msg)}

const browser=await chromium.launch({headless:true});
const context=await browser.newContext({viewport:{width:1440,height:900},geolocation:{latitude:-25.7479,longitude:28.2293},permissions:['geolocation','clipboard-read','clipboard-write']});
const imgPath=path.join(process.cwd(),'tests','qa-product.png');
const gen=await context.newPage();
await gen.setContent(`<html><body style="margin:0;background:#ececec"><canvas id="c" width="1200" height="800"></canvas><script>const c=document.getElementById('c'),x=c.getContext('2d');x.fillStyle='#ececec';x.fillRect(0,0,1200,800);x.fillStyle='#fff';x.fillRect(140,120,920,560);x.strokeStyle='#222';x.lineWidth=10;x.beginPath();x.moveTo(250,500);x.quadraticCurveTo(350,350,520,360);x.lineTo(720,410);x.quadraticCurveTo(860,445,920,520);x.lineTo(900,580);x.lineTo(300,580);x.quadraticCurveTo(230,565,250,500);x.closePath();x.fillStyle='#f7f7f7';x.fill();x.stroke();x.beginPath();x.moveTo(390,430);x.quadraticCurveTo(520,520,770,470);x.strokeStyle='#1d5cff';x.lineWidth=28;x.stroke();x.fillStyle='#111';x.font='bold 64px Arial';x.fillText('RUNNING SHOE',350,245);x.font='34px Arial';x.fillText('blue and white sneaker',420,300);</script></body></html>`);
await gen.locator('#c').screenshot({path:imgPath});await gen.close();

const page=await context.newPage(),pageErrors=[],consoleErrors=[],networkFailures=[];
page.on('pageerror',e=>pageErrors.push(String(e)));page.on('console',m=>{if(m.type()==='error')consoleErrors.push(m.text())});page.on('requestfailed',r=>networkFailures.push(`${r.method()} ${r.url()} ${r.failure()?.errorText||''}`));
page.on('dialog',async d=>{try{if(d.type()==='prompt')await d.accept('blue running shoe');else await d.accept()}catch{}});
context.on('page',async p=>{if(p!==page){try{await p.waitForLoadState('domcontentloaded',{timeout:3000})}catch{};try{await p.close()}catch{}}});

await safe('Production homepage loads',async()=>{const r=await page.goto(BASE_URL,{waitUntil:'domcontentloaded',timeout:35000});if(!r||r.status()!==200)throw Error(`HTTP ${r?.status()}`);await page.waitForTimeout(1600);return `HTTP ${r.status()}`});
await page.screenshot({path:path.join(OUT,'01-home-desktop.png')});
await safe('Desktop has no horizontal overflow',async()=>{const x=await page.evaluate(()=>({sw:document.documentElement.scrollWidth,cw:document.documentElement.clientWidth}));if(x.sw>x.cw+4)throw Error(JSON.stringify(x));return JSON.stringify(x)});
for(const [sel,name] of [['.desktop-nav a[href="#finder"]','Desktop nav Find'],['.desktop-nav a[href="#how"]','Desktop nav How'],['.desktop-nav a[href="#examples"]','Desktop nav Examples'],['.desktop-nav a[href="#feedback"]','Desktop nav Feedback']])await safe(name,async()=>{if(!await page.locator(sel).count())throw Error('missing')});
await click(page,'#menuBtn','Open main drawer');await safe('Main drawer opens',()=>expectVisible(page,'#drawer.open'));await safe('Main drawer controls do not overlap',async()=>{const b=await page.locator('#drawer .drawer-nav:first-of-type > a,#drawer .drawer-nav:first-of-type > button').evaluateAll(es=>es.filter(e=>getComputedStyle(e).display!=='none').map(e=>{const r=e.getBoundingClientRect();return {y:r.y,h:r.height,w:r.width,t:e.textContent.trim()}}));for(let i=1;i<b.length;i++)if(b[i].y<b[i-1].y+b[i-1].h-2)throw Error(`${b[i-1].t}/${b[i].t}`);return `${b.length} visible controls`});await click(page,'#closeMenu','Close main drawer');

await click(page,'#challengeBtn','Open Challenge');await safe('Challenge modal visible',()=>expectVisible(page,'#challengeModal:not(.hidden)'));await click(page,'#newChallenge','New challenge');await safe('Challenge text populated',async()=>{const t=await page.locator('#challengeText').innerText();if(!t.trim())throw Error('empty');return t});await closeKnown(page);
await safe('Examples render',async()=>{const n=await page.locator('#exampleGrid > *').count();if(n<4)throw Error(`${n}`);return `${n} cards`});await click(page,'#shuffleExamples','Shuffle examples');
await click(page,'#menuBtn','Open drawer for settings');await click(page,'#openSettings','Open Settings');await safe('Settings visible',()=>expectVisible(page,'#settingsModal:not(.hidden)'));await safe('Animations toggle works',async()=>{const e=page.locator('#animationsToggle');const a=await e.isChecked();await e.click();if(a===await e.isChecked())throw Error('no change')});await safe('Free radius options 3/5/10 work',async()=>{for(const v of ['3','5','10']){await page.locator('#settingsRadius').selectOption(v);if(await page.locator('#settingsRadius').inputValue()!==v)throw Error(v)}return '3,5,10'});await closeKnown(page);

await safe('Image picker accepts a real raster image',async()=>{await page.locator('#photo').setInputFiles(imgPath);await page.waitForTimeout(250);if(!await page.locator('#search').isEnabled())throw Error('Identify disabled');await expectVisible(page,'#preview:not(.hidden)')});
await click(page,'#location','Use my location',650);await safe('Location becomes ready',async()=>{const t=await page.locator('#location').innerText();if(!/Location ready/i.test(t))throw Error(t);return t});
await safe('Identify & Find completes',async()=>{await page.locator('#search').click();await page.waitForSelector('#results:not(.hidden)',{timeout:50000});await page.waitForTimeout(1300);const t=(await page.locator('#resultTitle').innerText()).trim();if(!t)throw Error('empty title');return t});
await page.screenshot({path:path.join(OUT,'02-search-results.png')});
await safe('Identification analysis cards render',async()=>{const n=await page.locator('#analysis .analysis-card').count();if(n<4)throw Error(`${n}`);return `${n}`});await click(page,'#mapViewBtn','Map view',650);await safe('Map view opens',async()=>{const c=await page.locator('#mapWrap').getAttribute('class');if(!String(c).includes('show'))throw Error(String(c));return c});await click(page,'#listViewBtn','List view');
for(const [v,n] of [['best','Offer sort Best'],['price','Offer sort Cheapest'],['distance','Offer sort Closest']])await safe(n,async()=>{const e=page.locator(`[data-sort="${v}"]`).first();if(await e.isVisible())await e.click();else return 'not applicable: no verified offers'});
await safe('Free result actions are truthful',async()=>{const fa=page.locator('#freeActions');if(await fa.isVisible()){for(const s of ['#searchOnline','#searchNearbyFree']){const h=await page.locator(s).getAttribute('href');if(!h?.startsWith('http'))throw Error(`${s} missing URL`)}await page.locator('#copyQuery').click();await page.locator('#shareFind').click();return 'search/copy/share available'}const t=(await page.locator('#resultTitle').innerText()).trim();return `low-confidence/result fallback shown instead of fake actions: ${t}`});
await safe('Product intelligence shows no fake zero price',async()=>{await expectVisible(page,'#productIntelligencePanel:not(.hidden)');const t=await page.locator('#productIntelligenceResults').innerText();if(/(?:ZAR|R)\s*0\.00/.test(t))throw Error('fake zero price');return t.slice(0,180)});
await click(page,'#thumbUp','Thumbs up');await safe('Thumbs up sets rating 5',async()=>{if(await page.locator('#feedbackRating').inputValue()!=='5')throw Error('not 5')});await click(page,'#thumbDown','Thumbs down');await safe('Thumbs down sets rating 2',async()=>{if(await page.locator('#feedbackRating').inputValue()!=='2')throw Error('not 2')});
await safe('Feedback form has empty-message validation',async()=>{await page.locator('#feedbackMessage').fill('');const invalid=await page.locator('#feedbackMessage').evaluate(e=>!e.checkValidity());if(invalid)return 'browser required validation active';await page.locator('#feedbackForm').evaluate(f=>f.requestSubmit());await page.waitForTimeout(120);const t=await page.locator('#feedbackStatus').innerText();if(!/detail|write|rating|more/i.test(t))throw Error(t||'no validation');return t});
await safe('Feedback copy works',async()=>{await page.locator('#feedbackMessage').fill('QA test only - not submitted');await page.locator('#copyFeedback').click();const t=await page.locator('#feedbackStatus').innerText();if(!/copied/i.test(t))throw Error(t);return t});

await safe('Free 25 km radius is Premium-gated',async()=>{await page.locator('#radiusSelect').selectOption('25');await page.waitForTimeout(160);await expectVisible(page,'#premiumModal:not(.hidden)');if(Number(await page.locator('#radiusSelect').inputValue())>10)throw Error('free radius exceeded 10');return 'gated'});await closeKnown(page);
await click(page,'#saveFind','Free Save opens Premium');await safe('Free Save gate visible',()=>expectVisible(page,'#premiumModal:not(.hidden)'));await closeKnown(page);
await click(page,'#premiumButton','Open Premium modal');await safe('Free/Premium comparison visible',async()=>{const t=await page.locator('#premiumModal').innerText();if(!t.includes('FindIt Free')||!t.includes('FindIt Premium'))throw Error('comparison missing')});await click(page,'#activatePremiumTester','Activate Premium Beta');await safe('Premium activates',async()=>{if(await page.evaluate(()=>localStorage.getItem('findit_premium_beta'))!=='1')throw Error('local premium flag missing');await expectVisible(page,'#v10CommandCentre:not(.hidden)');return 'active'});
await page.screenshot({path:path.join(OUT,'03-premium.png')});
await safe('Premium command centre fits desktop',async()=>{const x=await page.locator('#v10CommandCentre').evaluate(e=>({sw:e.scrollWidth,cw:e.clientWidth}));if(x.sw>x.cw+3)throw Error(JSON.stringify(x));return JSON.stringify(x)});await safe('Premium heading fits',async()=>{const b=await page.locator('#v10CommandCentre .v10-top h2').boundingBox();if(!b||b.width<500)throw Error(JSON.stringify(b));return `width=${Math.round(b.width)}`});await safe('All visible V10 tools include How guidance',async()=>{const bs=page.locator('#v10CommandCentre [data-v10]');const miss=[];for(let i=0;i<await bs.count();i++){const b=bs.nth(i);if(await b.isVisible()&&!await b.locator('.v10-how').count())miss.push(await b.getAttribute('data-v10'))}if(miss.length)throw Error(miss.join(','));return `${await bs.count()} tools`});await click(page,'#finditOpenGuide','Open Premium guide');await safe('Premium guide has full help',async()=>{await expectVisible(page,'#finditGuideModal:not(.hidden)');const n=await page.locator('#finditGuideModal article').count();if(n<10)throw Error(`${n}`);return `${n} entries`});await closeKnown(page);

for(const tool of ['scan','manual','exact','assistant','collections','watchlist','favourites','stats','history']){
 await closeKnown(page);
 await safe(`V10 ${tool} opens`,async()=>{const b=page.locator(`[data-v10="${tool}"]`).first();await b.scrollIntoViewIfNeeded();await b.click({timeout:4000});await page.waitForTimeout(220);if(tool==='scan')return 'routed to finder';if(tool==='exact'&&!await isVisible(page,'#v10UniversalModal:not(.hidden)'))return 'performed external exact-match action';if(tool==='assistant'&&!await isVisible(page,'#v10UniversalModal:not(.hidden)'))return 'performed assistant/search action';await expectVisible(page,'#v10UniversalModal:not(.hidden)');return (await page.locator('#v10ModalBody h2').innerText()).trim()});
 if(tool==='collections'&&await isVisible(page,'#v10CollectionName'))await safe('Collections create control works',async()=>{await page.locator('#v10CollectionName').fill('QA collection');await page.locator('#v10AddCollection').click();const t=await page.locator('#v10ModalBody').innerText();if(!t.includes('QA collection'))throw Error('collection not created');return 'created'});
 if(tool==='history'&&await isVisible(page,'#qaHistorySearch'))await safe('History+ supports per-item delete when entries exist',async()=>{await page.locator('#qaHistorySearch').fill('');const d=page.locator('[data-qa-history-delete]').first();if(await d.count()&&await d.isVisible()){const before=await page.locator('[data-qa-history-delete]').count();await d.click();return `${before} -> ${await page.locator('[data-qa-history-delete]').count()}`}return 'no history row available to delete'});
}
await closeKnown(page);

await safe('Premium drawer has visible, vertical controls',async()=>{await page.locator('#menuBtn').click();await page.waitForTimeout(120);const b=await page.locator('#premiumDrawerNav a,#premiumDrawerNav button').evaluateAll(es=>es.filter(e=>getComputedStyle(e).display!=='none').map(e=>{const r=e.getBoundingClientRect();return {y:r.y,h:r.height,t:e.textContent.trim()}}));if(b.length<8)throw Error(`${b.length}`);for(let i=1;i<b.length;i++)if(b[i].y<b[i-1].y+b[i-1].h-2)throw Error(`overlap ${b[i-1].t}/${b[i].t}`);return `${b.length} controls`});
const premiumRoutes=[['#premiumSavedMenu','Saved Items'],['#premiumCompareMenu','Compare Stores'],['#premiumFiltersMenu','Smart Filters'],['#premiumRadiusMenu','Search Radius'],['#premiumHistoryMenu','Extended History'],['#premiumChallengeMenu','Premium Challenge'],['#openSettingsPremium','Premium Settings']];
for(const [sel,name] of premiumRoutes){await closeKnown(page);await page.locator('#menuBtn').click();await page.waitForTimeout(80);await safe(`Premium drawer route: ${name}`,async()=>{const e=page.locator(sel).first();if(!await e.isVisible())throw Error('not visible');await e.click({timeout:3000});await page.waitForTimeout(180);return 'wired'});}
await closeKnown(page);await page.locator('#menuBtn').click();await safe('Price & Stock Watchlist route visible and wired',async()=>{const e=page.locator('#premiumDrawerNav button').filter({hasText:/Price|Stock/}).first();if(!await e.isVisible())throw Error('not visible');await e.click();await page.waitForTimeout(180);const t=await page.locator('body').innerText();if(!/price|stock/i.test(t))throw Error('watchlist did not open');return 'wired'});await closeKnown(page);

await click(page,'#assistantFab','Open Ask FindIt');await safe('Assistant opens',()=>expectVisible(page,'#assistantPanel:not(.hidden)'));await safe('Assistant quick prompt responds',async()=>{const before=await page.locator('#assistantMessages .assistant-msg').count();await page.locator('[data-assistant-quick]').first().click();await page.waitForTimeout(5000);const after=await page.locator('#assistantMessages .assistant-msg').count();if(after<=before)throw Error(`${before}->${after}`);return `${after} messages`},{warn:true});await closeKnown(page);

const mobile=await browser.newContext({viewport:{width:390,height:844},geolocation:{latitude:-25.7479,longitude:28.2293},permissions:['geolocation']});const mp=await mobile.newPage(),mobileErrors=[];mp.on('pageerror',e=>mobileErrors.push(String(e)));await safe('Mobile homepage loads',async()=>{const r=await mp.goto(BASE_URL,{waitUntil:'domcontentloaded',timeout:35000});if(!r||r.status()!==200)throw Error(String(r?.status()));await mp.waitForTimeout(1200)});await safe('Mobile has no horizontal overflow',async()=>{const x=await mp.evaluate(()=>({sw:document.documentElement.scrollWidth,cw:document.documentElement.clientWidth}));if(x.sw>x.cw+3)throw Error(JSON.stringify(x));return JSON.stringify(x)});await click(mp,'#menuBtn','Mobile menu opens');await safe('Mobile drawer controls vertical',async()=>{const b=await mp.locator('#drawer .drawer-nav:first-of-type > a,#drawer .drawer-nav:first-of-type > button').evaluateAll(es=>es.filter(e=>getComputedStyle(e).display!=='none').map(e=>{const r=e.getBoundingClientRect();return {y:r.y,h:r.height,w:r.width}}));for(let i=1;i<b.length;i++)if(b[i].y<b[i-1].y+b[i-1].h-2)throw Error('overlap');if(b.some(x=>x.w<250))throw Error('too narrow');return `${b.length}`});await safe('Mobile bottom nav visible',async()=>{const n=await mp.locator('.mobile-nav button,.mobile-nav a').count();if(n<3)throw Error(`${n}`);return `${n}`});await closeKnown(mp);await click(mp,'#mobileMore','Mobile More');await safe('Mobile More opens drawer',()=>expectVisible(mp,'#drawer.open'));
await mobile.close();

for(const [pathName,name] of [['api/health','Health API'],['api/feedback-health','Feedback health API']])await safe(`${name} live`,async()=>{const u=new globalThis.URL(pathName,BASE_URL).href;const r=await context.request.get(u);if(!r.ok())throw Error(`HTTP ${r.status()}`);const d=await r.json();if(!d.ok)throw Error(JSON.stringify(d));return JSON.stringify(d).slice(0,180)});
await safe('Built-in FindIt QA report passes',async()=>{const r=await page.evaluate(()=>window.finditRunQA?.());if(!r?.ok)throw Error(JSON.stringify(r));return JSON.stringify(r)});
await safe('No uncaught desktop JavaScript errors',async()=>{if(pageErrors.length)throw Error(pageErrors.join(' | '));return 'none'});await safe('No uncaught mobile JavaScript errors',async()=>{if(mobileErrors.length)throw Error(mobileErrors.join(' | '));return 'none'});await safe('No meaningful console errors',async()=>{const bad=consoleErrors.filter(x=>!/favicon|third-party|Failed to load resource/i.test(x));if(bad.length)throw Error(bad.slice(0,5).join(' | '));return 'none'});await safe('No unexpected network failures',async()=>{const bad=networkFailures.filter(x=>!/tile|analytics|fonts|google|jsdelivr/i.test(x));if(bad.length)throw Error(bad.slice(0,5).join(' | '));return 'none'});

const report={generatedAt:new Date().toISOString(),url:BASE_URL,summary:{passes:checks.filter(x=>x.status==='PASS').length,warnings:warnings.length,failures:failures.length},checks};fs.writeFileSync(path.join(OUT,'findit-full-audit.json'),JSON.stringify(report,null,2));fs.writeFileSync(path.join(OUT,'findit-full-audit.md'),`# FindIt Full Production Audit\n\nGenerated: ${report.generatedAt}\n\n**Passes:** ${report.summary.passes}  \n**Warnings:** ${report.summary.warnings}  \n**Failures:** ${report.summary.failures}\n\n${checks.map(x=>`- ${x.status==='PASS'?'✅':x.status==='WARN'?'⚠️':'❌'} **${x.name}**${x.detail?` — ${x.detail}`:''}`).join('\n')}\n`);await context.close();await browser.close();console.log(`AUDIT_SUMMARY=${JSON.stringify(report.summary)}`);if(failures.length)process.exit(1);