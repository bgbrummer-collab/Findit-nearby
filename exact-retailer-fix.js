/* FindIt Results Core v8.1 — identify -> universal exact listings -> nearby branches -> truthful price/stock scope. */
(()=>{
  'use strict';
  if(window.__finditResultsCoreV81)return;window.__finditResultsCoreV81=true;
  const $=s=>document.querySelector(s),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c])),clean=v=>String(v??'').trim(),norm=v=>clean(v).toLowerCase().replace(/[^a-z0-9]+/g,' ').replace(/\s+/g,' ').trim(),validUrl=v=>{try{const u=new URL(v);return /^https?:$/.test(u.protocol)}catch{return false}},S=()=>{try{return state}catch{return null}},hasCoords=s=>Boolean(s&&Number.isFinite(Number(s.lat))&&Number.isFinite(Number(s.lon)));
  let running=false,runId=0,officialRetailData=null;
  const style=document.createElement('style');style.id='findit-results-core-v81-style';style.textContent=`
    #exactSellerResults{width:100%;min-width:0;margin:20px 0 0}
    #exactSellerResults .offer-list{display:grid!important;grid-template-columns:1fr!important;gap:12px!important;width:100%!important}
    #exactSellerResults .offer-card{display:grid!important;grid-template-columns:minmax(0,1fr) auto!important;gap:20px!important;align-items:center!important;width:100%!important;min-width:0!important;padding:18px!important}
    #exactSellerResults .offer-card>div:first-child{min-width:0!important;width:auto!important}
    #exactSellerResults .offer-card h4{margin:0 0 6px!important;overflow-wrap:anywhere!important;font-size:16px!important;line-height:1.35!important}
    #exactSellerResults .offer-card p{margin:4px 0!important;line-height:1.45!important;overflow-wrap:anywhere!important}
    #exactSellerResults .offer-card a{display:inline-flex!important;margin-top:8px!important;white-space:normal!important}
    #exactSellerResults .offer-card .price{font-size:18px!important;font-weight:900!important;white-space:nowrap!important;text-align:right!important;align-self:center!important}
    #nearbyStores .store-card{min-width:0!important}
    #nearbyStores .store-main{min-width:0!important;width:100%!important}
    #nearbyStores .store-main small{white-space:normal!important;overflow-wrap:anywhere!important}
    #nearbyStores .store-actions{justify-content:flex-start!important;flex-wrap:wrap!important}
    @media(max-width:700px){#exactSellerResults .offer-card{grid-template-columns:1fr!important}.offer-card .price{text-align:left!important}.nearby-stores{grid-template-columns:1fr!important}}
  `;document.head.appendChild(style);
  const status=(txt,bad=false)=>{const e=$('#status');if(e){e.textContent=txt;e.style.color=bad?'#ff9da7':''}},radius=()=>{const n=Number(S()?.radius||$('#radiusSelect')?.value||10);return Number.isFinite(n)?n:10},money=(n,c='ZAR')=>{if(n==null||!Number.isFinite(Number(n)))return'Price not published';try{return new Intl.NumberFormat('en-ZA',{style:'currency',currency:c||'ZAR'}).format(Number(n))}catch{return`${c||'ZAR'} ${Number(n).toFixed(2)}`}},stockText=v=>{const x=String(v||'').toLowerCase();if(x==='in_stock')return'In stock online';if(x==='out_of_stock')return'Out of stock online';if(x==='preorder')return'Pre-order';if(x==='backorder')return'Back-order';return'Online stock not published'},mapUrl=s=>hasCoords(s)?`https://www.google.com/maps/search/?${new URLSearchParams({api:'1',query:`${Number(s.lat)},${Number(s.lon)}`})}`:'',directionsUrl=s=>hasCoords(s)?`https://www.google.com/maps/dir/?${new URLSearchParams({api:'1',destination:`${Number(s.lat)},${Number(s.lon)}`})}`:'';
  function getLocation(){return new Promise((resolve,reject)=>{if(!navigator.geolocation)return reject(new Error('Location unavailable'));navigator.geolocation.getCurrentPosition(p=>resolve({lat:p.coords.latitude,lon:p.coords.longitude}),reject,{enableHighAccuracy:true,timeout:10000,maximumAge:120000})})}
  async function postJson(url,body,timeout=15000){const c=new AbortController(),t=setTimeout(()=>c.abort(),timeout);try{const r=await fetch(url,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body),signal:c.signal});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||d.message||`${url} failed`);return d}finally{clearTimeout(t)}}
  function clearResults(){const s=S();if(s){s.result=null;s.offers=[];s.stores=[]}officialRetailData=null;$('#exactSellerResults')?.remove();if($('#nearbyStores'))$('#nearbyStores').innerHTML='';if($('#resultMeta'))$('#resultMeta').innerHTML='';if($('#resultNote'))$('#resultNote').textContent='';$('#results')?.classList.add('hidden')}
  function renderIdentification(i){const conf=Math.max(0,Math.min(100,Math.round(Number(i?.confidence||0)*100)));if($('#resultName'))$('#resultName').textContent=i?.name||i?.model||i?.object||'Item identified';if($('#resultDescription'))$('#resultDescription').textContent=i?.summary||'FindIt analysed the uploaded image.';if($('#confidenceValue'))$('#confidenceValue').textContent=`${conf}%`;const meta=$('#resultMeta');if(meta){const visible=Array.isArray(i?.visibleText)?i.visibleText.slice(0,5).join(' • '):clean(i?.visibleText);const rows=[['Object',i?.object],['Brand',i?.brand],['Model',i?.model],['Category',i?.retailCategory||i?.category],['Search',i?.searchQuery],['Visible text',visible]].filter(([,v])=>v);meta.innerHTML=rows.map(([k,v])=>`<div class="analysis-card"><span>${esc(k)}</span><strong>${esc(v)}</strong></div>`).join('')}const note=$('#resultNote');if(note){note.textContent=conf<55?'Try a clearer photo before trusting seller matches.':'';note.classList.toggle('error',conf<55)}}
  function ensureSellerRoot(){let root=$('#exactSellerResults');if(root)return root;root=document.createElement('section');root.id='exactSellerResults';root.className='exact-seller-results';const near=$('#nearbyPanel');if(near?.parentNode)near.parentNode.insertBefore(root,near);return root}
  function normalizeOffer(o){if(!o)return null;const retailer=clean(o.retailer?.name||o.retailer||'Retailer'),url=clean(o.product_url||o.url),title=clean(o.product_name||o.name||o.title),score=Number(o.matchScore??o.match??0),p=o.price==null?null:Number(o.price);return{retailer,url,title,score:Number.isFinite(score)?score:0,price:Number.isFinite(p)?p:null,currency:o.currency||'ZAR',availability:o.availability||null,verified:o.verified===true,source:o.source||''}}
  function officialOffers(d){if(!d?.exactMatch)return[];return(Array.isArray(d.retailerMatches)?d.retailerMatches:[]).map(x=>({retailer:{name:clean(x.retailerName)||'Retailer'},product_name:clean(x.listingTitle||d.productName),product_url:clean(x.sourceUrl),price:x.price==null?null:Number(x.price),currency:x.currency||'ZAR',availability:x.onlineAvailability||null,verified:Boolean(x.matchConfidence>=.8&&x.sourceUrl),matchScore:Number(x.matchConfidence||0),source:'Universal grounded exact-product retail search'})).filter(x=>x.verified&&x.product_name&&validUrl(x.product_url)&&(Number.isFinite(x.price)||x.availability))}
  function mergeOffers(rows){const out=[],seen=new Set();for(const raw of rows||[]){const o=normalizeOffer(raw);if(!o||!validUrl(o.url)||!o.title)continue;if(o.score&&o.score<.58)continue;const k=(o.retailer+'|'+o.title+'|'+o.url).toLowerCase();if(seen.has(k))continue;seen.add(k);out.push(o)}return out.sort((a,b)=>(Number(b.verified)-Number(a.verified))||(b.score-a.score)||((a.price??1e15)-(b.price??1e15))).slice(0,24)}
  function retailerLinks(d){const out=[];for(const x of Array.isArray(d?.webRetailers)?d.webRetailers:[]){if(validUrl(x.searchUrl)&&!out.some(y=>y.name===x.name))out.push({name:x.name,url:x.searchUrl})}return out.slice(0,8)}
  function renderSellers(d){window.productIntelligence=d||null;const root=ensureSellerRoot();if(!root)return;const offers=mergeOffers(d?.offers||[]),links=retailerLinks(d),priced=offers.filter(o=>o.price!=null).sort((a,b)=>a.price-b.price),best=priced[0];if(offers.length){root.innerHTML=`<div class="section-title-row"><div><p class="section-kicker">WHERE TO BUY</p><h3>${offers.length} live exact product match${offers.length===1?'':'es'}</h3><p>Prices and online stock come from verified retailer product pages. Branch stock is only labelled confirmed when branch-specific evidence proves it.</p></div></div>${best?`<div class="result-note"><strong>Best published price: ${esc(money(best.price,best.currency))}</strong> • ${esc(best.retailer)}</div>`:''}<div class="offer-list">${offers.map(o=>`<article class="offer-card"><div><h4>${esc(o.title)}</h4><p>${esc(o.retailer)}</p><p>✓ Exact product-page match • ${esc(stockText(o.availability))}</p><a href="${esc(o.url)}" target="_blank" rel="noopener noreferrer">Open product page →</a></div><div class="price">${esc(money(o.price,o.currency))}</div></article>`).join('')}</div>`}else{root.innerHTML=`<div class="section-title-row"><div><p class="section-kicker">WHERE TO BUY</p><h3>No exact live product page verified yet</h3><p>FindIt checked retailer pages and grounded web results but could not safely prove an exact current listing. It will not invent a price or stock status.</p></div></div>${links.length?`<div class="free-action-grid">${links.map(x=>`<a href="${esc(x.url)}" target="_blank" rel="noopener noreferrer">🛍️ <strong>${esc(x.name)}</strong><span>Search this exact product</span></a>`).join('')}</div>`:''}`}const s=S();if(s)s.offers=offers}
  function retailerKey(v){const n=norm(v);const aliases=[['pick n pay',['pick n pay','picknpay','pnp']],['checkers',['checkers']],['shoprite',['shoprite']],['makro',['makro']],['woolworths',['woolworths']],['clicks',['clicks']],['dis chem',['dis chem','dischem']],['incredible connection',['incredible connection','incredible']],['hifi corp',['hifi corp','hificorp']],['sportscene',['sportscene']],['totalsports',['totalsports','total sports']],['builders',['builders']],['game',['game']],['leroy merlin',['leroy merlin']]];for(const [key,vals] of aliases)if(vals.some(x=>n.includes(x)))return key;return n}
  function offerForStore(store){const sk=retailerKey(store?.name);if(!sk)return null;return(Array.isArray(S()?.offers)?S().offers:[]).find(o=>retailerKey(o.retailer)===sk)||null}
  function branchEvidenceForStore(store){const rows=Array.isArray(officialRetailData?.retailerMatches)?officialRetailData.retailerMatches:[],sn=norm(store?.name),sa=norm(store?.address),sk=retailerKey(store?.name);return rows.find(x=>{if(!x?.branchStockVerified||!x?.physicalBranch)return false;const rn=norm(x.retailerName),bn=norm(x.physicalBranch);const retailerOk=retailerKey(rn)===sk;const branchOk=bn&&(sn.includes(bn)||sa.includes(bn)||bn.includes(sn));return retailerOk&&branchOk})||null}
  function sizedItem(){const i=S()?.result?.identification||{};return /shoe|sneaker|footwear|clothing|shirt|dress|pants|jeans/i.test([i.category,i.retailCategory,i.object,i.name].filter(Boolean).join(' '))}
  function renderStores(list,verified,message=''){const el=$('#nearbyStores'),head=$('#nearbyPanel h3');if(!el)return;const stores=(Array.isArray(list)?list:[]).slice(0,12);if(!stores.length){if(head)head.textContent='Nearest relevant retailers';if($('#nearbySummary'))$('#nearbySummary').textContent='No relevant nearby retailer was found inside this radius.';el.innerHTML=`<div class="empty-state">${esc(message||'No relevant nearby retailer found inside this radius.')}</div>`;return}const anyGroundedBranch=stores.some(s=>Boolean(branchEvidenceForStore(s)));if(head)head.textContent=(verified||anyGroundedBranch)?'Exact item confirmed nearby':'Nearest relevant retailers';if($('#nearbySummary'))$('#nearbySummary').textContent=(verified||anyGroundedBranch)?'These branches have exact-item stock verified by branch-specific evidence.':'These are real nearby stores. Matching retailer prices are shown when FindIt has them; branch stock stays separate until verified.';el.innerHTML=stores.map((s,i)=>{const branchEvidence=branchEvidenceForStore(s),exact=Boolean((s?.exactProductMatch===true&&s?.stockVerified===true&&hasCoords(s))||branchEvidence),offer=offerForStore(s),dist=Number.isFinite(Number(s.distanceKm))?`${Number(s.distanceKm).toFixed(1)} km`:'Distance unavailable',map=mapUrl(s),dir=directionsUrl(s),phone=clean(s.phone),size=sizedItem()&&!exact?' Stock can depend on size.':'',branchPrice=exact&&s.branchPriceVerified===true&&Number.isFinite(Number(s.price))?Number(s.price):null,priceLine=branchPrice!=null?`<div class="result-note"><strong>${esc(money(branchPrice,s.currency||'ZAR'))}</strong> • verified branch price</div>`:offer?`<div class="result-note"><strong>${offer.price!=null?esc(money(offer.price,offer.currency)):'Price not published'}</strong> • verified online price at ${esc(offer.retailer)}</div>`:'';return`<article class="store-card" data-store="${i}" data-exact-branch="${exact?'1':'0'}"><div class="store-main"><strong>${esc(s.name||'Retailer')}</strong><small>${esc(dist+(s.address?' • '+s.address:''))}</small><div class="store-tags"><span>${exact?'✓ Exact product at branch':offer?'✓ Same retailer lists exact product online':'Relevant retailer'}</span><span>${exact?'✓ Branch stock confirmed':offer?esc(stockText(offer.availability)):'Branch stock not confirmed'}</span></div>${priceLine}<div class="store-actions">${offer?`<a href="${esc(offer.url)}" target="_blank" rel="noopener noreferrer">Product page</a>`:''}${validUrl(s.website)?`<a href="${esc(s.website)}" target="_blank" rel="noopener noreferrer">Website</a>`:''}${phone?`<a href="tel:${esc(phone.replace(/[^+0-9]/g,''))}">Call</a>`:''}${map?`<a href="${esc(map)}" target="_blank" rel="noopener noreferrer">Map</a>`:''}${dir?`<a href="${esc(dir)}" target="_blank" rel="noopener noreferrer">${exact?'Directions →':'Directions to store →'}</a>`:''}</div>${!exact?`<small class="store-trust-note">${offer?'Exact online listing found for this retailer; this physical branch has not confirmed stock.':'Exact product not confirmed at this branch.'}${esc(size)}</small>`:''}</div></article>`}).join('');const st=S();if(st)st.stores=stores}
  async function loadCommerce(i,id){const s=S(),body={query:i?.searchQuery||i?.name||i?.object||'',name:i?.name||i?.object||'',object:i?.object||'',brand:i?.brand||'',model:i?.model||'',category:i?.category||'',retailCategory:i?.retailCategory||'',lat:s?.coords?.lat,lon:s?.coords?.lon};const root=ensureSellerRoot();if(root)root.innerHTML='<div class="empty-state">Checking retailers and the live web for exact product, price and stock…</div>';const [core,grounded]=await Promise.allSettled([postJson('/api/product-intelligence-v2',body,35000),postJson('/api/official-brand-intelligence',body,35000)]);if(id!==runId)return;const d=core.status==='fulfilled'?core.value:{ok:true,offers:[]};officialRetailData=grounded.status==='fulfilled'?grounded.value:null;const combined=mergeOffers([...(Array.isArray(d?.offers)?d.offers:[]),...officialOffers(officialRetailData)]);d.offers=combined.map(o=>({retailer:{name:o.retailer},product_name:o.title,product_url:o.url,price:o.price,currency:o.currency,availability:o.availability,verified:o.verified,matchScore:o.score,source:o.source}));d.matched=d.offers.length>0;d.verifiedOfferCount=d.offers.length;d.verifiedSellerCount=new Set(d.offers.map(o=>o.retailer?.name).filter(Boolean)).size;const priced=d.offers.filter(o=>o.price!=null).sort((a,b)=>a.price-b.price);d.bestPrice=priced[0]||null;renderSellers(d);if(S()?.stores?.length)renderStores(S().stores,S().stores.some(x=>x?.exactProductMatch===true&&x?.stockVerified===true))}
  async function loadNearby(i,coords,id){if(!coords){renderStores([],false,'Allow location to see nearby retailers.');return}const body={lat:coords.lat,lon:coords.lon,identification:i,radiusKm:radius()};if($('#nearbyStores'))$('#nearbyStores').innerHTML='<div class="empty-state">Finding nearest relevant stores…</div>';const [exact,likely]=await Promise.allSettled([postJson('/api/nearby',body,15000),postJson('/api/nearby',{...body,mode:'likely'},15000)]);if(id!==runId)return;const ex=exact.status==='fulfilled'&&Array.isArray(exact.value?.stores)?exact.value.stores:[],lk=likely.status==='fulfilled'&&Array.isArray(likely.value?.stores)?likely.value.stores:[];if(ex.length)renderStores(ex,true);else if(lk.length)renderStores(lk,false);else renderStores([],false,likely.status==='fulfilled'?(likely.value?.message||'No relevant nearby retailer found inside this radius.'):'Nearby lookup could not finish. Try again or widen the radius.')}
  function saveRecent(i){try{const key='finditRecent',q=clean(i?.searchQuery||i?.name||i?.object);let a=JSON.parse(localStorage.getItem(key)||'[]');a=[{id:Date.now(),name:i?.name||i?.object||'Item',brand:i?.brand||'',query:q,date:new Date().toISOString()},...a.filter(x=>x.query!==q)].slice(0,20);localStorage.setItem(key,JSON.stringify(a))}catch{}}
  async function runSearch(e){const target=e.target?.closest?.('#search');if(!target)return;e.preventDefault();e.stopImmediatePropagation();if(running)return;const s=S();if(!s?.file){status('Choose a photo first.',true);return}running=true;const id=++runId;target.disabled=true;clearResults();status('Identifying the item…');try{if(!s.coords){try{s.coords=await getLocation();if($('#location'))$('#location').textContent='✓ Location ready'}catch{}}const fd=new FormData();fd.append('image',s.file);if(s.coords){fd.append('lat',s.coords.lat);fd.append('lon',s.coords.lon)}const c=new AbortController(),t=setTimeout(()=>c.abort(),25000);let r,data;try{r=await fetch('/api/search',{method:'POST',body:fd,signal:c.signal});data=await r.json().catch(()=>({}))}finally{clearTimeout(t)}if(!r.ok)throw new Error(data.message||data.error||`Search failed (${r.status})`);if(id!==runId)return;s.result=data;const i=data.identification||{};renderIdentification(i);$('#results')?.classList.remove('hidden');if(data.blocked){if($('#resultNote'))$('#resultNote').textContent=data.message||'This product cannot be searched.';status('Search complete.');return}if(Number(i.confidence||0)<.55){renderStores([],false,'Try a clearer photo before searching nearby stores.');status('Try a clearer photo.',true);return}status('Item identified. Checking exact listings, prices, stock and nearest stores…');await Promise.allSettled([loadCommerce(i,id),loadNearby(i,s.coords,id)]);if(id!==runId)return;renderStores(S()?.stores||[],(S()?.stores||[]).some(x=>x?.exactProductMatch===true&&x?.stockVerified===true));saveRecent(i);status('Search complete.');$('#results')?.scrollIntoView({behavior:'smooth',block:'start'});document.dispatchEvent(new CustomEvent('findit:results-rendered'))}catch(err){if(id===runId){$('#results')?.classList.remove('hidden');if($('#resultNote')){$('#resultNote').textContent=`Search error: ${err.name==='AbortError'?'The request took too long. Please try again.':err.message}`;$('#resultNote').classList.add('error')}status('Search failed. Please try again.',true)}}finally{if(id===runId){running=false;target.disabled=!S()?.file}}}
  document.addEventListener('click',runSearch,true);
})();