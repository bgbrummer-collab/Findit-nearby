name: FindIt Final Production Audit

on:
  push:
    branches: [main]
    paths:
      - 'index.html'
      - 'style.css'
      - 'script.js'
      - 'redesign-v4.css'
      - 'redesign-v4.js'
      - 'dashboard-runtime-fix.js'
      - 'scanner-flow.js'
      - 'journey-results-fix.js'
      - 'journey-results-v3-fix.js'
      - 'store-live-check.js'
      - 'mobile-menu-fix.js'
      - 'premium-upgrades.js'
      - 'qa-hardening.js'
      - 'qa-menu-routes.js'
      - 'qa-final-polish.js'
      - 'school-uniform-fix.js'
      - 'final-release-fixes.js'
      - 'api/**'
      - 'tests/release-smoke.mjs'
      - 'tests/dashboard-readiness.mjs'
      - 'tests/dashboard-all-functions.mjs'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 50
    steps:
      - name: Checkout production branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install Playwright
        run: |
          npm init -y >/dev/null 2>&1
          npm install --save-dev playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Wait for Vercel production
        run: sleep 40
      - name: Run stable pipeline smoke
        env:
          FINDIT_URL: https://findit-nearby.vercel.app/
        run: node tests/release-smoke.mjs
      - name: Run redesigned dashboard readiness audit
        env:
          FINDIT_URL: https://findit-nearby.vercel.app/
        run: node tests/dashboard-readiness.mjs
      - name: Test every functional dashboard control
        env:
          FINDIT_URL: https://findit-nearby.vercel.app/
        run: node tests/dashboard-all-functions.mjs
      - name: Upload audit output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: findit-final-production-audit-${{ github.run_id }}
          path: |
            audit-output/
            qa-results/
          if-no-files-found: warn
          retention-days: 14
