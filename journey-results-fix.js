/* FindIt fullscreen results — functional compare tabs, useful fallbacks, richer product info and truthful stock status. */
(()=>{
'use strict';
if(window.__finditJourneyResultsFixV3)return;window.__finditJourneyResultsFixV3=true;
const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
const esc=(v='')=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const validUrl=v=>{try{return /^https?:$/.test(new URL(v,location.href).protocol)}catch{return false}};
const root=()=>$('#finditJourneyV5');
const id=()=>{try{return window.state?.result?.identification||{}}catch{return{}}};
const query=()=>{const i=id();return [i.brand,i.model,i.searchQuery||i.query||i.name||i.object].filter(Boolean).join(' ').replace(/\s+/g,' ').trim()||$('#resultName')?.textContent?.trim()||'product'};
const money=(n,c='ZAR')=>{if(n==null||!Number.isFinite(Number(n)))return'';try{return new Intl.NumberFormat('en-ZA',{style:'currency',currency:c||'ZAR'}).format(Number(n))}catch{return `${c||'ZAR'} ${Number(n).toFixed(2)}`}};
const retailerDomains={checkers:'checkers.co.za','pick n pay':'pnp.co.za',pnp:'pnp.co.za',shoprite:'shoprite.co.za',woolworths:'woolworths.co.za',makro:'makro.co.za',game:'game.co.za',clicks:'clicks.co.za','dis chem':'dischem.co.za',builders:'builders.co.za','leroy merlin':'leroymerlin.co.za',adidas:'adidas.co.za',sportscene:'sportscene.co.za',totalsports:'totalsports.co.za','incredible connection':'incredible.co.za','hifi corp':'hificorp.co.za'};
function domainFor(name=''){const n=String(name).toLowerCase();return Object.entries(retailerDomains).find(([k])=>n.includes(k))?.[1]||''}
function safeSearch(name,domain){const q=`${query()} ${name||''}`.trim();return `https://www.google.com/search?q=${encodeURIComponent(domain?`site:${domain} ${q}`:q)}`}
function stateStores(){try{return Array.isArray(window.state?.stores)?window.state.stores:[]}catch{return[]}}
function domStores(){return $$('#nearbyStores .store-card,.nearby-stores .store-card').map((c,n)=>{const name=c.querySelector('strong,h3,h4')?.textContent?.trim()||`Store ${n+1}`;const addr=c.querySelector('small')?.textContent?.trim()||'';const txt=c.textContent.replace(/\s+/g,' ').trim();return{name,address:addr,stockVerified:/branch stock verified|stock verified/i.test(txt),branchPriceVerified:/verified branch price/i.test(txt)}}).filter(x=>x.name)}
function stores(){const rows=stateStores();return rows.length?rows:domStores()}
function allOffers(){const rows=[];try{if(Array.isArray(window.state?.offers))rows.push(...window.state.offers)}catch{}try{if(Array.isArray(window.productIntelligence?.offers))rows.push(...window.productIntelligence.offers)}catch{}const seen=new Set();return rows.filter(o=>{if(!o)return false;const k=`${o.retailer?.name||o.retailer||''}|${o.product_url||o.url||''}|${o.price??''}`;if(seen.has(k))return false;seen.add(k);return true})}
function verifiedOffers(){return allOffers().filter(o=>(o.verified===true||o.sourcePageVerified===true)&&Number.isFinite(Number(o.price))).sort((a,b)=>Number(a.price)-Number(b.price))}
function relevantRetailers(){const out=[];for(const s of stores()){const name=s.name||'Retailer';out.push({name,url:s.website||safeSearch(name,domainFor(name))})}try{for(const x of window.productIntelligence?.webRetailers||[])if(x?.name)out.push({name:x.name,url:x.searchUrl||safeSearch(x.name,domainFor(x.name))})}catch{}for(const a of $$('#exactSellerResults a[href],#freeActions a[href]'))if(validUrl(a.href))out.push({name:a.querySelector('strong')?.textContent?.trim()||a.textContent.replace(/Search this product|Open product page|→/gi,'').trim()||'Retailer',url:a.href});const seen=new Set();return out.filter(x=>{const k=String(x.name).toLowerCase();if(!x.url||seen.has(k))return false;seen.add(k);return true}).slice(0,8)}
function confidence(i){const n=Number(i?.confidence);if(Number.isFinite(n))return Math.round(n<=1?n*100:n);return String($('#confidenceValue')?.textContent||'—').replace(/[^0-9.]/g,'')||'—'}
function category(i){const g=String(i?.retailCategory||i?.category||'').trim();if(g&&!/^(product|item|general)$/i.test(g))return g;const s=[i?.object,i?.name,i?.model,i?.searchQuery].filter(Boolean).join(' ').toLowerCase();if(/toilet paper|bread|milk|cereal|food|grocery|detergent|cleaning/.test(s))return'Grocery / household';if(/speaker|headphone|phone|computer|camera|router|bluetooth/.test(s))return'Electronics';if(/toaster|kettle|microwave|vacuum|fridge/.test(s))return'Appliances';if(/shoe|sneaker|samba|footwear|trainer/.test(s))return'Footwear';if(/wrench|spanner|drill|hammer|screwdriver|pliers|tool/.test(s))return'Hardware / tools';if(/plug|adaptor|adapter|power strip|extension lead/.test(s))return'Electrical';if(/conditioner|shampoo|skincare|cosmetic|beauty/.test(s))return'Beauty / personal care';if(/car|vehicle|suv|sedan|coupe|mercedes/.test(s))return'Automotive';return g||'Product'}
function style(){if($('#finditJourneyResultsFixStyle'))return;const s=document.createElement('style');s.id='finditJourneyResultsFixStyle';s.textContent=`
#finditJourneyV5 .fj-tabs{display:grid;grid-template-columns:1fr 1fr;gap:4px;background:#10192a;border-radius:14px;padding:4px;margin:10px 0 14px}#finditJourneyV5 .fj-tabs button,#finditJourneyV5 .fj-tabs span{border:0;border-radius:10px;padding:10px;background:transparent;color:#8997aa;font:inherit;cursor:pointer}#finditJourneyV5 .fj-tabs .active{background:#1b4483;color:#fff}
#finditJourneyV5 .fj-price-helper{display:grid;gap:10px;margin:10px 0}#finditJourneyV5 .fj-price-helper .fj-card{margin:0}#finditJourneyV5 .fj-empty-help{padding:12px 13px;border:1px solid #202d43;border-radius:13px;background:#0d1728;color:#a8b4c7;font-size:11px;line-height:1.5}
#finditJourneyV5 .fj-stock-row{display:flex;align-items:center;justify-content:space-between;gap:10px;margin-top:10px;flex-wrap:wrap}#finditJourneyV5 .fj-stock-unknown{display:inline-flex;padding:5px 8px;border-radius:99px;background:#172238;color:#a9b7ca;font-size:10px}#finditJourneyV5 .fj-check-stock{border:0;border-radius:10px;background:#15233a;color:#7ee5b4;padding:9px 11px;font-weight:800;cursor:pointer}
#finditJourneyV5 .fj-fix-grid{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin:10px 0}#finditJourneyV5 .fj-fix-cell{padding:10px;border:1px solid #202d43;border-radius:11px;background:#0d1728;min-width:0}#finditJourneyV5 .fj-fix-cell span,#finditJourneyV5 .fj-fix-cell strong{display:block}#finditJourneyV5 .fj-fix-cell span{color:#8493aa;font-size:10px}#finditJourneyV5 .fj-fix-cell strong{margin-top:4px;font-size:12px;overflow-wrap:anywhere}#finditJourneyV5 .fj-fix-note{padding:11px 12px;border-radius:12px;background:#0d1728;color:#9fb0c9;font-size:11px;line-height:1.45;margin:10px 0}
`;document.head.appendChild(s)}
function clearCompareContent(r){$$('.fj-card,.fj-fix-note,.fj-price-helper',r).forEach(x=>x.remove())}
function insertBeforeBack(r,html){const page=$('.fj-page',r),back=$('.fj-secondary[data-fj="actions"]',r);if(!page)return;const box=document.createElement('div');box.innerHTML=html;while(box.firstChild)page.insertBefore(box.firstChild,back||null)}
function renderPriceMode(mode){const r=root();if(!r)return;clearCompareContent(r);if(mode==='online'){const priced=verifiedOffers();if(priced.length){insertBeforeBack(r,priced.slice(0,12).map(o=>{const name=o.retailer?.name||o.retailer||'Retailer',url=o.product_url||o.url||'',availability=String(o.availability||'').replace(/_/g,' ');return `<div class="fj-card"><div class="fj-row"><div><h3>${esc(name)}</h3><div class="fj-green">Verified current listing</div>${availability?`<div class="fj-muted">${esc(availability)}</div>`:''}</div><div class="fj-price">${esc(money(o.price,o.currency||'ZAR'))}</div></div>${validUrl(url)?`<button class="fj-link" data-fix-open="${esc(url)}">View product</button>`:''}</div>`}).join(''));return}const rs=relevantRetailers();insertBeforeBack(r,`<div class="fj-price-helper"><div class="fj-empty-help"><b>Current price is still being verified.</b><br>FindIt will not guess a price. Check the identified item at relevant retailers below while verification continues.</div>${rs.map(x=>`<div class="fj-card"><h3>${esc(x.name)}</h3><div class="fj-muted">Price not verified by FindIt yet</div><button class="fj-link" data-fix-open="${esc(x.url)}">Check current price</button></div>`).join('')}</div>`)}else{const all=stores(),verified=all.filter(s=>s.branchPriceVerified===true&&Number.isFinite(Number(s.price)));if(verified.length){insertBeforeBack(r,verified.slice(0,12).map(s=>`<div class="fj-card"><div class="fj-row"><div><h3>${esc(s.name||'Store')}</h3><div class="fj-green">Verified branch price</div><div class="fj-muted">${esc([Number.isFinite(Number(s.distanceKm))?`${Number(s.distanceKm).toFixed(1)} km`:'',s.address||''].filter(Boolean).join(' • '))}</div></div><div class="fj-price">${esc(money(s.price,s.currency||'ZAR'))}</div></div></div>`).join(''));return}if(all.length){insertBeforeBack(r,`<div class="fj-price-helper"><div class="fj-empty-help"><b>No verified branch price yet.</b><br>These are relevant nearby retailers. Branch stock and branch price remain unverified until the retailer confirms them.</div>${all.slice(0,8).map(s=>{const name=s.name||'Store',stock=s.stockVerified===true,link=s.website||safeSearch(name,domainFor(name));return `<div class="fj-card"><h3>${esc(name)}</h3><div class="fj-muted">${esc([Number.isFinite(Number(s.distanceKm))?`${Number(s.distanceKm).toFixed(1)} km`:'',s.address||''].filter(Boolean).join(' • '))}</div><div class="fj-stock-row"><span class="fj-stock-unknown">${stock?'✓ Branch stock verified':'Stock unknown'}</span><button class="fj-check-stock" data-fix-open="${esc(link)}">Check stock / price</button></div></div>`}).join('')}</div>`)}else insertBeforeBack(r,'<div class="fj-empty-help"><b>No nearby branch data was returned yet.</b><br>Open Nearest Stores or use the Online tab to check relevant retailers.</div>')}}
function patchPrices(r){const h=$('.fj-head b',r);if(!h||h.textContent.trim()!=='Compare Prices')return false;const tabs=$('.fj-tabs',r);if(!tabs)return false;if(!tabs.querySelector('[data-fix-price]'))tabs.innerHTML='<button type="button" class="active" data-fix-price="online">Online</button><button type="button" data-fix-price="store">In-store</button>';const active=$('[data-fix-price].active',tabs)||$('[data-fix-price="online"]',tabs);const hasUseful=verifiedOffers().length||$('.fj-price-helper',r);if(!hasUseful)renderPriceMode(active?.dataset.fixPrice||'online');return true}
function patchProduct(r){const h=$('.fj-head b',r);if(!h||h.textContent.trim()!=='Product Information'||r.dataset.productEnhanced==='1')return;r.dataset.productEnhanced='1';const i=id(),page=$('.fj-page',r),back=$('.fj-secondary[data-fj="actions"]',r);if(!page)return;const rows=[['Brand',i.brand],['Model / variant',i.model],['Category',category(i)],['Search identity',i.searchQuery||i.query||i.name||i.object],['Identity status',i.exactIdentityVerified===true?'Photo identity verified':'AI identification — retailer verification separate']].filter(([,v])=>v),features=Array.isArray(i.features)?i.features.filter(Boolean).slice(0,8):[],visible=Array.isArray(i.visibleText)?i.visibleText.filter(Boolean).slice(0,8):[];const wrap=document.createElement('div');wrap.className='findit-product-extra';wrap.innerHTML=`<div class="fj-card"><h3>Product details</h3><div class="fj-fix-grid">${rows.map(([k,v])=>`<div class="fj-fix-cell"><span>${esc(k)}</span><strong>${esc(v)}</strong></div>`).join('')}</div></div>${features.length?`<div class="fj-card"><h3>Key details</h3>${features.map(x=>`<div class="fj-muted">✓ ${esc(x)}</div>`).join('')}</div>`:''}${visible.length?`<div class="fj-card"><h3>Text visible on the product</h3><div class="fj-muted">${visible.map(esc).join(' • ')}</div></div>`:''}<div class="fj-fix-note">Identification confidence: ${esc(confidence(i))}%. Product identity, online price, branch stock and branch price are separate checks.</div>`;page.insertBefore(wrap,back||null)}
function patchStores(r){const h=$('.fj-head b',r);if(!h||h.textContent.trim()!=='Nearest Stores')return;const empty=$('.fj-map-empty',r);if(empty&&empty.dataset.fixed!=='1'){const s=stores().find(x=>Number.isFinite(Number(x.lat))&&Number.isFinite(Number(x.lon)));if(s){const url=`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(`${s.lat},${s.lon}`)}`;empty.innerHTML=`Map preview is unavailable here.<br><button class="fj-link" data-fix-open="${esc(url)}">Open nearest store map</button>`;empty.dataset.fixed='1'}}$$('.fj-card',r).forEach(card=>{const badge=[...card.querySelectorAll('.fj-badge')].find(x=>/stock not verified|stock unknown/i.test(x.textContent||''));if(!badge||card.querySelector('.fj-stock-row'))return;const name=card.querySelector('h3,strong')?.textContent?.trim()||'Store';const row=document.createElement('div');row.className='fj-stock-row';row.innerHTML=`<span class="fj-stock-unknown">Branch stock not verified</span><button class="fj-check-stock" data-fix-open="${esc(safeSearch(name,domainFor(name)))}">Check stock</button>`;badge.replaceWith(row)})}
let retrying=false;async function tryRefresh(){if(retrying||verifiedOffers().length)return;retrying=true;try{const i=id(),body={...i,query:i.searchQuery||i.query||i.name||i.model||i.object,searchQuery:i.searchQuery||i.query||i.name||i.model||i.object};const c=new AbortController(),t=setTimeout(()=>c.abort(),14000);const res=await fetch('/api/product-intelligence-v2',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body),signal:c.signal});clearTimeout(t);if(res.ok){const data=await res.json();window.productIntelligence=data;if(Array.isArray(data.offers)){try{window.state.offers=data.offers}catch{}}const r=root();if(r&&$('.fj-head b',r)?.textContent.trim()==='Compare Prices'&&($('[data-fix-price].active',r)?.dataset.fixPrice||'online')==='online')renderPriceMode('online')}}catch{}finally{retrying=false}}
let queued=false;function sync(){const r=root();if(!r||r.classList.contains('hidden'))return;style();const isPrices=patchPrices(r);patchProduct(r);patchStores(r);if(isPrices&&($('[data-fix-price].active',r)?.dataset.fixPrice||'online')==='online')tryRefresh()}function queue(){if(queued)return;queued=true;requestAnimationFrame(()=>{queued=false;sync()})}
document.addEventListener('click',e=>{const tab=e.target.closest?.('[data-fix-price],#finditJourneyV5 .fj-tabs button,#finditJourneyV5 .fj-tabs span');if(tab&&tab.closest?.('.fj-tabs')){let mode=tab.dataset?.fixPrice;if(!mode){const t=(tab.textContent||'').toLowerCase();mode=t.includes('in-store')||t.includes('in store')?'store':t.includes('online')?'online':''}if(mode){e.preventDefault();e.stopImmediatePropagation();const r=root();patchPrices(r);$$('[data-fix-price]',r).forEach(b=>b.classList.toggle('active',b.dataset.fixPrice===mode));renderPriceMode(mode);if(mode==='online')tryRefresh();return}}const open=e.target.closest?.('[data-fix-open]');if(open){e.preventDefault();e.stopImmediatePropagation();if(validUrl(open.dataset.fixOpen))window.open(open.dataset.fixOpen,'_blank','noopener')}},true);
function init(){style();new MutationObserver(queue).observe(document.body,{childList:true,subtree:true});document.addEventListener('findit:results-rendered',queue);setInterval(sync,900);sync()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();