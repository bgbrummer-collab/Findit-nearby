import {resolveProduct} from '../lib/product-resolver.js';
import {searchSimilar} from '../lib/similar-search.js';
import {augmentRetailerCoverage} from '../lib/retailer-coverage.js';
import {selectedRetailers,hostMatches} from '../lib/retailers.js';

const BLOCKED=/\b(firearm|gun|rifle|pistol|ammunition|ammo|weapon|knife|machete|sword|switchblade|taser|pepper spray|fireworks|explosive|vape|nicotine|cigarette|alcohol|beer|wine|liquor|cannabis|marijuana|thc|cbd|gambling|casino|pornography)\b/i;
const conditioner={brand:'Marc Anthony',model:'Strictly Curls 3X Moisture Triple Blend Conditioner 250ml',name:'Marc Anthony Strictly Curls 3X Moisture Triple Blend Conditioner 250ml',query:'Marc Anthony Strictly Curls 3X Moisture Triple Blend Conditioner 250ml',searchQuery:'Marc Anthony Strictly Curls 3X Moisture Triple Blend Conditioner 250ml',object:'conditioner',category:'hair care',retailCategory:'beauty',features:['curl conditioner','moisture','detangling','250ml']};
const norm=v=>String(v??'').toLowerCase().replace(/\b(\d+)\s*(ml|mg|g|kg|l|gb|tb|cm|mm|inch|in)\b/g,'$1$2').replace(/[^a-z0-9]+/g,' ').replace(/\s+/g,' ').trim();
const toks=v=>[...new Set(norm(v).split(' ').filter(x=>x.length>2&&!['the','and','for','with','from','this','that','search','product','item','online','shop','buy','men','mens','women','womens'].includes(x)))];
const TYPE_GROUPS=[['footwear',['shoe','shoes','sneaker','sneakers','trainer','trainers','footwear','boot','boots']],['conditioner',['conditioner','hair conditioner']],['shampoo',['shampoo']],['headphones',['headphone','headphones','headset','headsets','earbuds','earphones']],['router',['router','wifi router','wi fi router','range extender','wifi extender','wi fi extender']],['phone',['phone','smartphone','mobile phone']],['laptop',['laptop','notebook']],['tablet',['tablet']],['television',['television','tv','smart tv']],['camera',['camera']],['monitor',['monitor','display']],['keyboard',['keyboard']],['mouse',['mouse']],['speaker',['speaker']],['charger',['charger','charging adapter','power adapter']],['adapter',['adapter','adaptor']],['microwave',['microwave']],['kettle',['kettle']],['toaster',['toaster']],['vacuum',['vacuum','vacuum cleaner']],['drill',['drill']],['hammer',['hammer']],['jacket',['jacket','coat']],['shirt',['shirt','t shirt','tshirt','top']],['jeans',['jeans','denim']],['watch',['watch','smartwatch']],['perfume',['perfume','fragrance']],['serum',['serum']],['mascara',['mascara']],['foundation',['foundation']],['lipstick',['lipstick']],['toothpaste',['toothpaste']],['deodorant',['deodorant']],['detergent',['detergent']],['cleaner',['cleaner','cleaning']]];
const parsePrice=v=>{if(v==null)return null;let s=String(v).replace(/\s/g,'').replace(/[^0-9.,]/g,'');if(!s)return null;if(s.includes(',')&&s.includes('.'))s=s.replace(/,/g,'');else if(s.includes(',')&&!s.includes('.')){const p=s.split(',');s=p.at(-1).length===2?p.slice(0,-1).join('')+'.'+p.at(-1):p.join('')}const n=Number(s);return Number.isFinite(n)&&n>0&&n<10000000?n:null};
const parseStock=v=>{const s=norm(v);if(/out of stock|sold out|currently unavailable|not available/.test(s))return'out_of_stock';if(/in stock|available online|add to cart|add to basket|delivery available|click collect|click and collect|store pickup|store pick up/.test(s))return'in_stock';if(/pre.?order/.test(s))return'preorder';if(/back.?order/.test(s))return'backorder';return null};
const parseJson=text=>{const s=String(text||'').replace(/^```json\s*/i,'').replace(/```\s*$/,'').trim();try{return JSON.parse(s)}catch{const a=s.indexOf('{'),z=s.lastIndexOf('}');if(a>=0&&z>a){try{return JSON.parse(s.slice(a,z+1))}catch{}}return null}};
const directProductUrl=(url,domain)=>{try{const u=new URL(url),p=u.pathname.toLowerCase();if(!hostMatches(url,domain))return false;if(/\/(search|catalogsearch|browse|category|categories|brands?|collections?|all)(\/|$)/.test(p))return false;if(/[?&](q|text|search|qsearch)=/i.test(u.search))return false;if(domain==='clicks.co.za')return /\/p\/\d+\/?$/.test(p);return p.split('/').filter(Boolean).length>=1}catch{return false}};

function typeAliases(body){const source=norm([body.object,body.category,body.retailCategory,body.name].filter(Boolean).join(' '));for(const [,aliases] of TYPE_GROUPS)if(aliases.some(a=>source.includes(norm(a))))return aliases.map(norm);return[]}
function typeCompatible(hay,body){const a=typeAliases(body);return !a.length||a.some(x=>hay.includes(x))}
function profileFor(url,retailerName,body){const q=body.searchQuery||body.query||body.name||body.model||body.object||'';const profiles=selectedRetailers(q,body);return profiles.find(p=>hostMatches(url,p.domain))||profiles.find(p=>norm(p.name)===norm(retailerName))||null}
function brandCompatible(hay,url,retailerName,body){const brand=norm(body.brand);if(!brand)return true;if(hay.includes(brand))return true;const p=profileFor(url,retailerName,body),pb=norm(p?.brand);return Boolean(pb&&(brand.includes(pb)||pb.includes(brand)))}
function exactEnough(title,url,body,profile){const hay=norm(`${title||''} ${url||''}`),brand=norm(body.brand),pb=norm(profile?.brand);if(brand&&!hay.includes(brand)&&!(pb&&(brand.includes(pb)||pb.includes(brand))))return false;if(!typeCompatible(hay,body))return false;const wanted=toks(body.searchQuery||body.query||body.name||body.model||body.object),model=toks(body.model),sizes=wanted.filter(t=>/^\d+(?:\.\d+)?(?:ml|mg|g|kg|l|gb|tb|cm|mm|inch|in)$/.test(t));if(sizes.length&&!sizes.every(x=>hay.includes(x)))return false;const typeSet=new Set(typeAliases(body));const meaningfulModel=model.filter(t=>!typeSet.has(t)&&t!==brand);if(meaningfulModel.length&&meaningfulModel.filter(t=>hay.includes(t)).length<Math.min(2,meaningfulModel.length))return false;const hits=wanted.filter(t=>hay.includes(t)).length;return hits>=Math.min(4,Math.max(2,Math.ceil(wanted.length*.32)))}
function urlLooksExact(o,body){if(o?.listingType!=='retailer_search_verified'&&o?.source!=='Exact retailer search result')return true;try{const url=o.product_url||'',path=norm(decodeURIComponent(new URL(url).pathname)),hay=norm(`${o.product_name||''} ${path}`);if(!typeCompatible(hay,body))return false;if(!brandCompatible(hay,url,o?.retailer?.name,body))return false;const q=toks(body.searchQuery||body.query||body.name||body.model||body.object),hits=q.filter(t=>hay.includes(t)).length;return hits>=Math.min(4,Math.max(2,Math.ceil(q.length*.32)))}catch{return false}}
function sanitizeExact(result,body){if(!result?.offers?.length)return result;let offers=result.offers.filter(o=>urlLooksExact(o,body));offers=offers.filter(o=>typeCompatible(norm(`${o.product_name||''} ${o.product_url||''}`),body));offers=offers.filter(o=>brandCompatible(norm(`${o.product_name||''} ${o.product_url||''}`),o.product_url,o?.retailer?.name,body));result.offers=offers;result.matched=offers.length>0;result.verifiedOfferCount=offers.length;result.verifiedSellerCount=new Set(offers.map(o=>o.retailer?.name).filter(Boolean)).size;result.bestProduct=offers[0]?{name:offers[0].product_name}:null;const priced=offers.filter(o=>o.price!=null).sort((a,b)=>a.price-b.price);result.bestPrice=priced[0]||null;return result}
function mergeOffers(result,extra){const seen=new Set(),rows=[];for(const o of [...(result.offers||[]),...(extra||[])]){const k=norm(`${o.retailer?.name||''}|${o.product_url||''}`);if(!k||seen.has(k))continue;seen.add(k);rows.push(o)}rows.sort((a,b)=>Number(b.matchScore||0)-Number(a.matchScore||0)||((a.price??1e15)-(b.price??1e15)));result.offers=rows.slice(0,30);result.matched=rows.length>0;result.verifiedOfferCount=rows.length;result.verifiedSellerCount=new Set(rows.map(o=>o.retailer?.name).filter(Boolean)).size;result.bestProduct=rows[0]?{name:rows[0].product_name}:null;const priced=rows.filter(o=>o.price!=null).sort((a,b)=>a.price-b.price);result.bestPrice=priced[0]||null;return result}

const decodeHtml=s=>String(s||'').replace(/&quot;/gi,'"').replace(/&#39;|&apos;/gi,"'").replace(/&amp;/gi,'&').replace(/&lt;/gi,'<').replace(/&gt;/gi,'>');
const stripHtml=s=>decodeHtml(String(s||'').replace(/<script[\s\S]*?<\/script>/gi,' ').replace(/<style[\s\S]*?<\/style>/gi,' ').replace(/<[^>]+>/g,' ').replace(/\s+/g,' '));
function metaValue(html,key){const re1=new RegExp(`<meta[^>]+(?:property|name|itemprop)=["']${key.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')}["'][^>]+content=["']([^"']+)["']`,'i');const re2=new RegExp(`<meta[^>]+content=["']([^"']+)["'][^>]+(?:property|name|itemprop)=["']${key.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')}["']`,'i');return decodeHtml((html.match(re1)||html.match(re2)||[])[1]||'')}
function pageTitle(html){return metaValue(html,'og:title')||metaValue(html,'twitter:title')||decodeHtml((html.match(/<title[^>]*>([\s\S]*?)<\/title>/i)||[])[1]||'').trim()}
function jsonLdProducts(html){const rows=[];for(const m of html.matchAll(/<script[^>]+type=["']application\/ld\+json["'][^>]*>([\s\S]*?)<\/script>/gi)){try{const d=JSON.parse(decodeHtml(m[1]).trim());const walk=x=>{if(!x)return;if(Array.isArray(x))return x.forEach(walk);if(typeof x!=='object')return;const type=Array.isArray(x['@type'])?x['@type'].join(' '):String(x['@type']||'');if(/product/i.test(type))rows.push(x);Object.values(x).forEach(v=>{if(v&&typeof v==='object')walk(v)})};walk(d)}catch{}}return rows}
function productOfferFromLd(products){for(const p of products){const os=Array.isArray(p.offers)?p.offers:[p.offers].filter(Boolean);for(const o of os){const price=parsePrice(o?.price??o?.lowPrice??o?.highPrice),stock=parseStock(o?.availability);if(price!=null||stock)return{name:p.name||'',price,availability:stock,currency:o?.priceCurrency||'ZAR'}}}return{name:'',price:null,availability:null,currency:'ZAR'}}
function distinctiveTokens(body){const generic=new Set(['nike','adidas','running','shoe','shoes','sneaker','sneakers','black','white','blue','red','green','footwear','product','item','athletic','sports']);const rows=[...(Array.isArray(body.visibleText)?body.visibleText:[]),...(Array.isArray(body.evidence)?body.evidence:[]),...(Array.isArray(body.features)?body.features:[])];return [...new Set(rows.flatMap(toks).filter(t=>t.length>=5&&!generic.has(t)))].slice(0,8)}
function sourcePageMatches(title,bodyText,url,body,profile,ldName=''){const core=norm(`${title} ${ldName} ${url}`),full=norm(`${core} ${bodyText}`),brand=norm(body.brand),pb=norm(profile?.brand);if(brand&&!full.includes(brand)&&!(pb&&(brand.includes(pb)||pb.includes(brand))))return false;if(!typeCompatible(full,body))return false;const model=toks(body.model),typeSet=new Set(typeAliases(body)),modelKeys=model.filter(t=>!typeSet.has(t)&&t!==brand);if(modelKeys.length){const mh=modelKeys.filter(t=>full.includes(t)).length;if(mh<Math.min(2,modelKeys.length))return false}const sizes=toks(body.searchQuery||body.query||body.name).filter(t=>/^\d+(?:\.\d+)?(?:ml|mg|g|kg|l|gb|tb|cm|mm|inch|in)$/.test(t));if(sizes.length&&!sizes.every(t=>full.includes(t)))return false;const q=toks(body.searchQuery||body.query||body.name||body.object),coreHits=q.filter(t=>core.includes(t)).length,fullHits=q.filter(t=>full.includes(t)).length,clueHits=distinctiveTokens(body).filter(t=>full.includes(t)).length;return coreHits>=Math.min(3,Math.max(2,Math.ceil(q.length*.28)))||fullHits>=Math.min(4,Math.max(2,Math.ceil(q.length*.45)))||clueHits>0}
async function verifySourceOffer(o,body){const url=o?.product_url;if(!url)return null;const p=profileFor(url,o?.retailer?.name,body);if(!p||!directProductUrl(url,p.domain))return null;try{const r=await fetch(url,{headers:{'user-agent':'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 Chrome/131 Safari/537.36','accept':'text/html,application/xhtml+xml','accept-language':'en-ZA,en;q=0.9'},redirect:'follow',signal:AbortSignal.timeout(9000)});if(!r.ok)return null;const ct=r.headers.get('content-type')||'';if(!/html|xhtml|json/.test(ct))return null;const html=(await r.text()).slice(0,1400000),title=pageTitle(html),products=jsonLdProducts(html),ld=productOfferFromLd(products),ldName=products.map(x=>x?.name).filter(Boolean)[0]||'';const text=stripHtml(html).slice(0,300000);if(!sourcePageMatches(title,text,r.url||url,body,p,ldName))return null;const metaPrice=parsePrice(metaValue(html,'product:price:amount')||metaValue(html,'og:price:amount')||metaValue(html,'price')),metaCurrency=metaValue(html,'product:price:currency')||metaValue(html,'og:price:currency');const pagePrice=ld.price??metaPrice,pageStock=ld.availability??parseStock(metaValue(html,'availability')||text.slice(0,120000));return{...o,product_name:ldName||title||o.product_name,product_url:r.url||url,price:pagePrice,currency:ld.currency||metaCurrency||o.currency||'ZAR',availability:pageStock,verified:true,source:'Verified directly on retailer product page',sourcePageVerified:true,checkedAt:new Date().toISOString()}}catch{return null}}
function trustedWithoutPage(o){const s=norm(`${o?.source||''} ${o?.retailer?.source||''}`);return /connected retailer feed|catalog|partner feed|retailer api/.test(s)}
async function verifyAllOffersAtSource(result,body){const rows=result?.offers||[];if(!rows.length)return result;const trusted=rows.filter(trustedWithoutPage),untrusted=rows.filter(o=>!trustedWithoutPage(o)).slice(0,18);const checked=[];for(let i=0;i<untrusted.length;i+=6){const batch=await Promise.all(untrusted.slice(i,i+6).map(o=>verifySourceOffer(o,body)));checked.push(...batch.filter(Boolean))}result.offers=[];return mergeOffers(result,[...trusted,...checked])}

async function universalGroundedRecovery(body,result){const key=process.env.GEMINI_API_KEY;if(!key)return result;const q=String(body.searchQuery||body.query||body.name||body.model||body.object||'').trim();if(!q)return result;const profiles=selectedRetailers(q,body).slice(0,24),existing=new Set((result.offers||[]).map(o=>norm(o.retailer?.name)).filter(Boolean));const missing=profiles.filter(p=>!existing.has(norm(p.name))||!(result.offers||[]).some(o=>norm(o.retailer?.name)===norm(p.name)&&o.price!=null));if(!missing.length)return result;const allowed=missing.map(p=>`${p.name} (${p.domain})${p.brand?` [official ${p.brand} store]`:''}`).join(', ');const prompt=`Use Google Search only to DISCOVER candidate direct product pages for the exact current product "${q}" at these South African retailers: ${allowed}. Match brand, model/variant, visible model/technology text and important size/capacity identifiers. Never substitute a different model. Return direct product pages only. Price and stock values are hints only because FindIt will independently verify the retailer page. JSON only: {"offers":[{"retailer":"","title":"","url":"","price":null,"currency":"ZAR","availability":"in stock|out of stock|preorder|backorder|unknown","confidence":0}]}.`;let parsed=null;for(const model of ['gemini-2.5-flash','gemini-2.5-flash-lite']){try{const r=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${model}:generateContent`,{method:'POST',headers:{'content-type':'application/json','x-goog-api-key':key},body:JSON.stringify({contents:[{parts:[{text:prompt}]}],tools:[{google_search:{}}],generationConfig:{temperature:0}}),signal:AbortSignal.timeout(22000)});const d=await r.json().catch(()=>({}));if(!r.ok)continue;parsed=parseJson((d?.candidates?.[0]?.content?.parts||[]).map(x=>x.text||'').join(''));if(parsed?.offers?.length)break}catch{}}if(!parsed?.offers?.length)return result;const out=[];for(const x of parsed.offers){const p=missing.find(y=>norm(y.name)===norm(x.retailer)||hostMatches(x.url,y.domain));if(!p||Number(x.confidence||0)<.62||!directProductUrl(x.url,p.domain))continue;out.push({product_name:String(x.title||q),price:null,currency:x.currency||'ZAR',availability:null,product_url:x.url,verified:false,source:'Grounded discovery candidate — source page verification required',retailer:{name:p.name,country:'ZA',source:'retailer_site'},matchScore:Number(x.confidence||0),branchStockVerified:false,branchPriceVerified:false,stockScope:'online',priceScope:'online',directionsAvailable:false,listingType:'retailer_candidate',onlineListing:true,checkedAt:new Date().toISOString()})}return mergeOffers(result,out)}

export default async function handler(req,res){res.setHeader('Cache-Control','no-store');let body=req.body||{};if(req.method==='GET'&&req.query?.test==='conditioner')body={...conditioner};else if(req.method==='GET'&&req.query?.test==='alternatives')body={...conditioner,action:'alternatives'};else if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});if(BLOCKED.test([body.query,body.searchQuery,body.name,body.object,body.category,body.retailCategory].filter(Boolean).join(' ')))return res.status(403).json({error:'Unsupported product type'});try{if(body.action==='alternatives'){const result=await searchSimilar(body);if(!result.ok&&result.error)return res.status(400).json(result);return res.status(200).json(result)}let result=await resolveProduct(body);if(!result.ok&&result.error)return res.status(400).json(result);result=await augmentRetailerCoverage(result,body);result=sanitizeExact(result,body);result=await universalGroundedRecovery(body,result);result=await verifyAllOffersAtSource(result,body);result=sanitizeExact(result,body);return res.status(200).json(result)}catch(error){console.error('FindIt product resolver error',error);return res.status(500).json({ok:false,error:'Product lookup failed',message:error?.message||'Unknown error'})}}
