const BLOCKED=/\b(firearm|gun|rifle|pistol|ammunition|ammo|weapon|knife|knives|machete|sword|switchblade|taser|stun gun|pepper spray|mace|brass knuckles|fireworks|explosive|vape|nicotine|cigarette|cigar|alcohol|beer|wine|liquor|cannabis|marijuana|thc|cbd|psilocybin|magic mushroom|gambling|sports betting|casino|betting|pornography|adult sex toy)\b/i;
const clean=v=>String(v??'').trim();
const norm=v=>clean(v).toLowerCase().replace(/&amp;/g,' and ').replace(/[^a-z0-9]+/g,' ').replace(/\s+/g,' ').trim();
const STOP=new Set(['the','and','for','with','from','this','that','new','white','black','small','large','pack','set','product','item']);
const toks=v=>[...new Set(norm(v).split(' ').filter(x=>x.length>1&&!STOP.has(x)))];
const price=v=>{if(v==null)return null;let s=String(v).replace(/\s/g,'').replace(/[^0-9.,]/g,'');if(!s)return null;if(s.includes(',')&&s.includes('.'))s=s.replace(/,/g,'');else if(s.includes(',')&&!s.includes('.')){const a=s.split(',');s=a.at(-1).length===2?a.slice(0,-1).join('')+'.'+a.at(-1):a.join('')}const n=Number(s);return Number.isFinite(n)&&n>0&&n<10000000?n:null};
const stock=v=>{const x=norm(v);if(/out of stock|sold out|unavailable|not available/.test(x))return'out_of_stock';if(/in stock|available|add to cart|buy now|ships in|delivery/.test(x))return'in_stock';if(/preorder|pre order/.test(x))return'preorder';return null};
const abs=(href,base)=>{try{return new URL(href,base).href}catch{return null}};
const host=u=>{try{return new URL(u).hostname.replace(/^www\./,'')}catch{return''}};
const strip=s=>clean(String(s||'').replace(/<script[\s\S]*?<\/script>/gi,' ').replace(/<style[\s\S]*?<\/style>/gi,' ').replace(/<[^>]+>/g,' ').replace(/&nbsp;|&#160;/gi,' ').replace(/&quot;/gi,'"').replace(/&#39;|&apos;/gi,"'").replace(/&amp;/gi,'&').replace(/\s+/g,' '));
function queryFrom(b={}){const parts=[b.brand,b.model,b.searchQuery||b.query,b.name||b.object].map(clean).filter(Boolean);return [...new Set(parts)].join(' ').replace(/\s+/g,' ').trim().slice(0,180)}
function strong(text,q,b={}){const n=norm(text);if(!n)return false;const brand=norm(b.brand),model=norm(b.model);if(brand&&!n.includes(brand))return false;if(model&&!n.includes(model))return false;const ts=toks(q);const hits=ts.filter(t=>n.includes(t)).length;return hits>=Math.min(4,Math.max(2,Math.ceil(ts.length*.45)))}
function walk(n,out=[]){if(!n)return out;if(Array.isArray(n)){n.forEach(x=>walk(x,out));return out}if(typeof n!=='object')return out;const t=Array.isArray(n['@type'])?n['@type'].join(' '):n['@type'];if(/product/i.test(String(t||'')))out.push(n);for(const k of ['@graph','itemListElement','item'])if(n[k])walk(n[k],out);return out}
function structuredProducts(html,base,q,b,retailer){const out=[],re=/<script[^>]+type=["']application\/ld\+json["'][^>]*>([\s\S]*?)<\/script>/gi;let m;while((m=re.exec(html))){try{const d=JSON.parse(m[1].trim().replace(/&quot;/g,'"'));for(const p of walk(d)){const name=clean(p.name||p.item?.name),off=Array.isArray(p.offers)?p.offers[0]:(p.offers||{}),txt=[name,p.brand?.name||p.brand,p.model,p.sku,p.mpn,p.description].join(' ');if(!name||!strong(txt,q,b))continue;const url=abs(p.url||off.url||base,base)||base;out.push({retailer,product_name:name,price:price(off.price||off.lowPrice),currency:clean(off.priceCurrency||'ZAR')||'ZAR',availability:stock(off.availability),product_url:url,verified:true,source:'Live retailer structured data',checkedAt:new Date().toISOString(),branchStockVerified:false,stockScope:'online'})}}catch{}}return out}
async function fetchHtml(url,timeout=6500){try{const r=await fetch(url,{headers:{'User-Agent':'Mozilla/5.0 (compatible; FindItNearby/8.0)','Accept':'text/html,application/xhtml+xml','Accept-Language':'en-ZA,en;q=0.9'},redirect:'follow',signal:AbortSignal.timeout(timeout)});if(!r.ok||!(r.headers.get('content-type')||'').includes('text/html'))return null;return{url:r.url||url,html:(await r.text()).slice(0,1600000)}}catch{return null}}
function jsonFromText(text){const t=clean(text).replace(/^```(?:json)?/i,'').replace(/```$/,'').trim();try{return JSON.parse(t)}catch{}const a=t.indexOf('{'),z=t.lastIndexOf('}');if(a>=0&&z>a){try{return JSON.parse(t.slice(a,z+1))}catch{}}return null}
async function grounded(key,q,b){const prompt=`Search the current South African web for direct retailer PRODUCT PAGES for this exact product: ${q}. Prioritise exact brand and model. Return at most 8 offers. Do not return homepages, category pages, search pages or a merely similar product. Price and online stock must be null unless the source currently shows them. Never claim physical branch stock. Return JSON only: {"offers":[{"retailer":"","title":"","url":"","price":null,"currency":"ZAR","availability":null,"confidence":0}]}.`;for(const model of ['gemini-3.6-flash','gemini-3.5-flash-lite']){try{const r=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${model}:generateContent`,{method:'POST',headers:{'content-type':'application/json','x-goog-api-key':key},body:JSON.stringify({contents:[{parts:[{text:prompt}]}],tools:[{google_search:{}}],generationConfig:{temperature:.01}}),signal:AbortSignal.timeout(12000)});const d=await r.json().catch(()=>({}));if(!r.ok)continue;const text=(d?.candidates?.[0]?.content?.parts||[]).map(x=>x.text||'').join('');const parsed=jsonFromText(text);if(!parsed?.offers?.length)continue;const chunks=(d?.candidates?.[0]?.groundingMetadata?.groundingChunks||[]).map(x=>x?.web).filter(x=>x?.uri);return {offers:parsed.offers,sources:chunks}}catch{}}return{offers:[],sources:[]}}
async function verifyCandidate(c,q,b,sources){let url=clean(c.url);const retailer=clean(c.retailer||host(url)||'Retailer');if(!/^https?:/i.test(url)){const src=sources.find(s=>norm(s.title).includes(norm(retailer)));url=clean(src?.uri)}if(!/^https?:/i.test(url))return null;const h=host(url);const grounded=sources.some(s=>host(s.uri)===h||host(s.uri).endsWith('.'+h)||h.endsWith('.'+host(s.uri)));if(!grounded)return null;const page=await fetchHtml(url);if(!page)return null;const structured=structuredProducts(page.html,page.url,q,b,retailer);if(structured[0])return structured[0];const txt=strip(page.html).slice(0,180000);if(!strong(txt,q,b))return null;let p=price(c.price);if(p!=null){const a=String(Math.round(p)),b2=String(p.toFixed(2));if(!txt.replace(/\s/g,'').includes(a)&&!txt.replace(/\s/g,'').includes(b2.replace('.','')))p=null}return{retailer,product_name:clean(c.title||q),price:p,currency:clean(c.currency||'ZAR')||'ZAR',availability:stock(c.availability||txt),product_url:page.url,verified:true,source:'Live grounded retailer page',checkedAt:new Date().toISOString(),branchStockVerified:false,stockScope:'online'} }
function dedupe(rows){const seen=new Set(),out=[];for(const x of rows){const k=norm(`${x.retailer}|${x.product_name}|${x.product_url}`);if(!k||seen.has(k))continue;seen.add(k);out.push(x)}return out}
export default async function handler(req,res){res.setHeader('Cache-Control','no-store');if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});try{const b=req.body||{},q=queryFrom(b);if(!q)return res.status(400).json({error:'Product details required'});if(BLOCKED.test([q,b.category,b.retailCategory].join(' ')))return res.status(403).json({error:'Unsupported product type'});const key=process.env.GEMINI_API_KEY;if(!key)return res.status(500).json({error:'Live product lookup is not configured'});const g=await grounded(key,q,b);const checked=await Promise.all((g.offers||[]).slice(0,8).map(x=>verifyCandidate(x,q,b,g.sources||[])));const offers=dedupe(checked.filter(Boolean)).slice(0,8);return res.status(200).json({ok:true,query:q,offers,verifiedOfferCount:offers.length,verifiedPriceCount:offers.filter(x=>x.price!=null).length,verifiedOnlineStockCount:offers.filter(x=>x.availability==='in_stock').length,checkedAt:new Date().toISOString(),message:offers.length?'Live exact-product retailer pages verified.':'No current exact retailer product page could be verified automatically.'})}catch(e){console.error('live-commerce',e);return res.status(200).json({ok:true,offers:[],message:'Live product lookup is temporarily limited.',error:clean(e.message)})}}
